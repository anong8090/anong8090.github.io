# 2026-07-25

## 按大纲 v3 优化「智能采购比价与合同管理系统介绍_极简价值版.html」
- 对齐大纲 v3 文案：P2 解法卡补全 4 条解释；P3 重构为「左三条进数据 → 中列四维水位 chips → 右两条出价格」+ 列间流动箭头；P12 补「投资回收期 1~3 个月」行。
- P4–P7 占位符换成真实截图（system_screenshots/contract.png、forecast_dash.png、matrix_dash.png、materials.png），用 `.real-shot { object-fit: contain }` 保证界面完整可见（cover 会裁掉 20%+ 内容）。
- P8 看板动效补全：原 CSS 引用 fadeIn 但从未定义（动画一直失效），补 @keyframes fadeIn/drawLine；Sparkline 描画动画；价格 countUp 滚动（支持 1 位小数）；材料加单位（吨/米/m³/m²）。
- 修复隐性 bug：`:root` 缺 --gray-400/500/600 定义，导致 P9 虚线框、P11 灰徽章（白字透明底会隐形）失效，已补齐 slate 色阶。
- 验证：node --check JS 语法 OK；HTML 标签配平 OK。
- 环境备忘：本机 Chrome headless 截图在沙箱内 GPU FATAL、sandbox 外被 SIGKILL，不可用；验证静态页面用「起 python http.server + 静态检查」即可。中文文件名 URL 需 quote 编码，且 curl 要加 --noproxy "*"（Shadowrocket 会劫持 localhost 返回 502）。
